# Google Places API (New) — for auto-fetching Google reviews into the homepage
# See scripts/README-google-reviews.md for setup instructions.
# These are READ at build time by scripts/fetch-google-reviews.mjs.
# Never commit the real values to git — copy this file to .env.local and fill in.

GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
